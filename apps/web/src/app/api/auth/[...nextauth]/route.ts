import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json({ message: 'Auth.js route placeholder for session auth integration' })
}

export async function POST() {
  return NextResponse.json({ message: 'Auth.js route placeholder for session auth integration' })
}
